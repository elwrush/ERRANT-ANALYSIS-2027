---
description: Git backup — stage, review, commit with Conventional Commits, push to remote, and verify
agent: build
---

Load and follow the global `git-backup` skill.

Support the optional `--dry-run` argument: run the full workflow but skip `git commit` and `git push`, presenting what would happen instead.

Arguments: $ARGUMENTS
