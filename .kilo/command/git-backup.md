---
description: Git backup — stage, review, commit with Conventional Commits, push to remote, and verify
---

Load the `git-backup` skill from `.kilo/skills/git-backup/SKILL.md` and follow the workflow.

Usage: `/git-backup` or `/git-backup --dry-run` (show what would happen without committing).
